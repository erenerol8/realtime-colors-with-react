import React from "react";

const Palette = () => {
  return (
    <div className="palette">
      <input type="color"></input>
      <button>click</button>
    </div>
  );
};

export default Palette;
